# Яндекс Минус — Распределение задач по участникам

> **Запуск проекта:** `npm install && npm run dev`
> **Стек:** React 18 + Vite 5 + Zustand (стейт) + React Router (роутинг)
> **Общий стейт:** `src/store/gameStore.js` — **НЕ ТРОГАТЬ без согласования**

---

## Подготовка к работе

```bash
git clone <repo-url>
cd smehsmehsmeh
npm install
npm run dev  # откроется http://localhost:5173
```

Каждый работает **в своей ветке** (`git checkout -b feature/имя-задачи`), мёрж через PR.

---

## 👤 Участник 1 — Онбординг & Регистрация

**Зона ответственности:** Первое впечатление пользователя. Всё, что до главного экрана.

### Файлы

- `src/pages/Registration.jsx` — основная страница (каркас готов)
- `src/pages/Registration.css` — стили (каркас готов)
- `src/components/ui/DifficultCaptcha.jsx` — капча (заглушка)
- `src/components/minigames/LicenseReader.jsx` — чтение лицензии вслух (заглушка)

### Задачи

- [ ] **Капча из 2003 года** — `DifficultCaptcha.jsx`
  - Canvas с рандомным текстом (5-6 символов)
  - Искажение: rotate, skew, полосы, шум
  - 3 попытки, потом комментарий «Может, ты робот?»
  - Должно **выглядеть отвратительно** (это суть)

- [ ] **Выбор гендера** — красивые карточки (вместо выбора класса)
  - Гендеры уже в `gameStore.js` (Боевой вертолет Апач, Истинный скуф, Масик, Тюбик и т.д.)
  - Оформлено как стандартный выбор класса в RPG
  - Эффекты: свечение при выборе, тряска при наведении

- [ ] **Чтение лицензии вслух** — `LicenseReader.jsx`
  - `webkitSpeechRecognition` / `SpeechRecognition` API
  - Показать текст + подсветка распознанных слов в реальном времени
  - Порог: 70%+ совпадения → разблокировать «Принять»
  - Вызвать `gameStore.completeLicense()` при успехе (+100 ЯР)

- [ ] **Визуальная полировка** регистрации
  - Glitch-эффект на заголовке «ЯНДЕКС МИНУС»
  - Анимации переходов между шагами (fade / slide)
  - Страшилка-popup если юзер пытается закрыть страницу

### API контракт

```js
// После регистрации вызвать:
import { useGameStore } from '../store/gameStore';
const register = useGameStore(s => s.register);
register({ userName, userClass: 'mage', monthlyIncome: 50000 });

// После лицензии:
const completeLicense = useGameStore(s => s.completeLicense);
completeLicense(); // +100 ЯР
```

---

## 👤 Участник 2 — Главный экран & Рулетки & Заказ

**Зона ответственности:** Главная страница, визуальная деградация UI, рулетки, заказ.

### Файлы

- `src/pages/Home.jsx` — главный экран (каркас готов)
- `src/pages/Order.jsx` — заказ поездки (каркас готов)
- `src/components/ui/RunawayButton.jsx` — убегающая кнопка (каркас готов)
- `src/components/roulette/DriverRoulette.jsx` — барабан водителей (заглушка)
- `src/components/roulette/PriceRoulette.jsx` — рулетка цен (заглушка)
- `src/styles/global.css` — **тут уже 5 тем (data-level)**, дорабатывать можно

### Задачи

- [ ] **Убегающая кнопка** — `RunawayButton.jsx`
  - Каркас готов, нужно довести: touch-events, ограничение в пределах экрана
  - На уровне 0: убегает всегда
  - На уровне 1: убегает первые 2 секунды, потом стоит
  - На уровне 2+: стоит на месте

- [ ] **Рекламный баннер** — новый компонент `AdvertBanner.jsx`
  - Уровень 0: 80% экрана, не закрывается 15 сек, потом крестик
  - Уровень 1: 40% экрана, закрывается сразу
  - Уровень 2+: нет рекламы
  - Или: закрыть за 10 ЯР (кнопка «Купить закрытие рекламы»)

- [ ] **Рулетка водителей (кейс как в CS:GO)** — `DriverRoulette.jsx`
  - Рулетка запускается **сразу после нажатия кнопки «Заказать»**.
  - Горизонтальный барабан с карточками (flex + overflow hidden + translateX).
  - Механика кейсов рандомно (с весами) выбирает вам водителя.
  - Подсветка по редкости. При выпадении "Дед на Жигулях" — шансон.
  - **Только после того, как водитель выбран, он выводится на карту.**

- [ ] **d20 бросок перед заказом** — визуальный 3D или 2D кубик
  - При попытке нажать "Заказать" крутится кубик.
  - **Если выпадает 1 (критическая неудача):** блокируется поездка на такси и выводится огромный баннер: **"Езжай на автобусе нищеброд"**.
  - Результат влияет на цену (если > 1)

- [ ] **Детектор устройства** — плашка на главном экране
  - `navigator.userAgent` → iPhone / Android
  - Если iPhone: поездка автоматически дороже (+20%). Если Android: дешевле (-10%).
  - Анимированная плашка с текстом.

- [ ] **Режим «ВМЕСТЕ»** (Отряд для файта)
  - Вместо обычного «Вместе», пользователь собирает отряд для драки с водителем (танки, хилеры).
  - Дебафф: если добавить «ребенка» в отряд, водитель злится и шанс доехать падает.

- [ ] **Тариф Антикомфорт и Антискидка**
  - Тариф Антикомфорт: в машине играет шансон, окно не закрывается, цена х2.
  - Антискидка: на самый первый заказ вместо 500 рублей платишь 10 000 рублей.

- [ ] **Поездка из Нидерландов (VPN)**
  - Если детектируется VPN (или просто случайный рандом 5%), приложение считает, что ты в Амстердаме.
  - Маршрут строится из Нидерландов в Москву, цена в евро, время ожидания: 4 дня.

- [ ] **Адрес-слайдеры** (ур.0-1) — доработать `Order.jsx`
  - Два слайдера: широта/долгота
  - Компас «холодно-тепло» по расстоянию до Москвы

### API контракт

```js
import { useRideStore } from '../store/rideStore';
// После заказа:
useRideStore.getState().setAddress(address, fromSVO);
useRideStore.getState().setDriver(driverCard);
useRideStore.getState().setD20(d20Result);
useRideStore.getState().startWaiting();
```

---

## 👤 Участник 3 — Мини-игры & CV & Поддержка & Ожидание

**Зона ответственности:** Все мини-игры, CV-распознавание, поддержка, экран ожидания.

### Файлы

- `src/pages/Waiting.jsx` — экран ожидания (каркас готов)
- `src/pages/Support.jsx` — поддержка (каркас готов)
- `src/components/minigames/SudokuModal.jsx` — судоку (заглушка)
- `src/components/minigames/GestureCapture.jsx` — CV-жест (заглушка)
- `src/components/minigames/PedalTapper.jsx` — педали (заглушка)
- `src/components/minigames/BalanceGame.jsx` — алкотестер (заглушка)

### Задачи

- [ ] **Судоку для отмены** — `SudokuModal.jsx`
  - Генератор судоку (есть npm-пакеты: `sudoku` или написать свой)
  - Сетка 9×9 с навигацией по клеткам
  - Сложность зависит от `cancelCount` (см. ТЗ)
  - +30 ЯР за решение, −20 ЯР за сдачу
  - Вызвать: `gameStore.addRubles(30, 'Судоку решено')` / `addRubles(-20, 'Судоку сдался')`

- [ ] **Тапай хомяка** — `PedalTapper.jsx`
  - Визуальный метроном: пульсирующий круг с целевым BPM (например, 120)
  - При тапе: проверить, попал ли ±200ms в бит
  - Показывать % попаданий
  - < 50% → анимация «водитель делает крюк»
  - Изображение хомяка на которое нужно тапать

- [ ] **CV-жест «67»** — `GestureCapture.jsx`
  - `navigator.mediaDevices.getUserMedia({ video: true })`
  - Подключить MediaPipe Hands или вашу готовую CV-модель
  - Распознать жест → вызвать `addRubles(67, 'Жест 67')` (разово)

- [ ] **Алкотестер** — `BalanceGame.jsx`
  - DeviceMotion API: `window.addEventListener('devicemotion', ...)`
  - Или fallback для десктопа: мышью двигать шарик в круг
  - 5 сек стабильности = прошёл

- [ ] **Поддержка** — полировка `Support.jsx`
  - Бот на ур.0-2: отвечает чушь с задержкой 2-5 сек
  - Бот на ур.2+: нормальные ответы (захардкоженные FAQ)
  - Ур.3: «живой чат» (но это всё ещё бот, просто с аватаркой)
  - Анимация «бот печатает...» (три точки)

- [ ] **Интерактивная карта Москвы, Спавн водителя и Симуляция**
  - На экране ожидания такси отображается реальная карта Москвы.
  - Как только кейс (рулетка) определил водителя, **отрисовывается его случайный спавн** на карте.
  - От водителя к пользователю **строится маршрут и симулируется его движение по карте**.
  - На этом маршруте генерируются рандомные покемоны. Маршрут строится с обязательным заездом на них.
  - Пока симуляция водителя не проедет и не соберет всех покемонов по пути, поездка не сможет завершиться.

### API контракт

```js
import { useGameStore } from '../store/gameStore';
const addRubles = useGameStore(s => s.addRubles);
const incrementCancel = useGameStore(s => s.incrementCancel);

// Судоку решено:
addRubles(30, 'Судоку решено');
// Отмена:
incrementCancel();
addRubles(-30, 'Отмена поездки');
```

---

## 👤 Участник 4 — Магазин & Колода & Статистика & Деградация

**Зона ответственности:** Внутриигровая экономика, коллекционирование, аналитика, визуальная деградация.

### Файлы

- `src/pages/Shop.jsx` — магазин (каркас готов)
- `src/pages/CardDeck.jsx` — колода (каркас готов)
- `src/pages/Stats.jsx` — статистика (каркас готов)

### Задачи

- [ ] **Магазин улучшений** — полировка `Shop.jsx`
  - Красивые карточки с иконками
  - Анимация покупки: confetti / glow / shake
  - «Недостаточно ЯР!» — модалка с грустным эмодзи
  - Визуальное отображение «до/после» покупки
  - Каталог улучшений уже захардкожен, можно расширить

- [ ] **Колода водителей (Гвинт)** — полировка `CardDeck.jsx`
  - За *каждого* водителя, с которым ты проехал поездку, тебе дается его уникальная карточка (срабатывает при `completeRide`).
  - Карточки с рамками по редкости (gold glow для legendary)
  - Flip-анимация при получении новой карточки
  - Фильтры: все / common / rare / epic / legendary
  - Счётчик: «12/50 собрано»
  - Карточка: имя, характеристики (⚡скорость, 🤫тишина, 🎵музыкальность)

- [ ] **Статистика деградации** — полировка `Stats.jsx`
  - Графики: ЯР во времени (canvas или SVG)
  - «Шкала боли»: визуальная полоска 0-100% деградации
  - Лог с фильтрами (+ / − / все)
  - Забавные комментарии к статистике

- [ ] **Боготворение Яндекса (Повышение рейтинга)**
  - Чтобы повысить себе рейтинг (отдельный стат), нужно ввести текстовое предложение (символ в символ), боготворящее Яндекс.
  - Каждая ошибка сбрасывает ввод.

- [ ] **Механика «Амнезия»** — интеграция
  - При потере стрика: приложение забывает имя (уже в store)
  - Лог: «Кто ты? Мы тебя не помним»
  - Кнопка «Купить Память» → 100 ЯР → `cureAmnesia()`

- [ ] **Механика «Похмелье»** — CSS-эффекты
  - 3+ отменов → `filter: blur(3px)` + shake (уже частично в global.css)
  - Подключить `BalanceGame` для «отрезвления»
  - Визуальный индикатор «вам плохо» на главном экране

### API контракт

```js
import { useGameStore } from '../store/gameStore';
const buyUpgrade = useGameStore(s => s.buyUpgrade);
const cureAmnesia = useGameStore(s => s.cureAmnesia);

// Покупка:
const ok = buyUpgrade('colorPalette', 50); // true/false
// Амнезия:
cureAmnesia();
```

---

## 👤 Участник 5 — Презентация & Дизайн & Контент (не программист)

**Зона ответственности:** Питч, дизайн-ассеты, тексты, UX-flow, деплой, тестирование.

### Задачи

#### Питч-презентация

- [ ] **Слайды** (Google Slides / Figma) — 5-7 минут
  - Открывашка: «Яндекс даёт скидки за лояльность. Мы решили, что недостаточно жестоко»
  - Демо-flow: регистрация → заказ → рулетка → ожидание → статистика
  - Техническая страница: стек, архитектура, система прогресса
  - Вау-момент: попросить жюри прочитать лицензию вслух → аппа реагирует
  - Закрывашка: скриншоты всех уровней UI (от ада до нормы)

- [ ] **Скрипт выступления** — по разделам, с тайм-кодами

#### Дизайн и ассеты

- [ ] **Логотип «Яндекс Минус»** (перевёрнутый логотип Яндекса?)
- [ ] **GIF-анимации** для уровня 0 (мерцающие машинки, spinning taxi)
- [ ] **Иконки гендеров** (Скуф, Тюбик, Вертолет Апач и т.д.)
- [ ] **Карточки водителей** — шаблон (Figma/Canva)
- [ ] **Фоны рекламных баннеров** (чем угарнее, тем лучше)

#### Тексты и контент

- [ ] **Лицензионное соглашение** — полный текст (3 абзаца, смешной)
- [ ] **Фразы бота поддержки** — 20+ вариантов (пассивная агрессия)
- [ ] **Push-уведомления** — 10+ вариантов
- [ ] **Описания водителей** для карточек (имя + байка)
- [ ] **Описания улучшений** в магазине (с юмором)
- [ ] **Названия улиц** — расширить банк до 200+ (файл `src/data/streets.json`)
- [ ] **Темы для чата «Вместе»** — компрометирующие вопросы

#### Тестирование и деплой

- [ ] **Тестирование user flow** — пройти весь путь, записать видео
- [ ] **Деплой на Vercel** — `npx vercel --prod`
- [ ] **README.md** проекта — описание для GitHub
- [ ] **Запись демо-видео** для защиты (если требуется)

#### Куда складывать контент

```
src/data/streets.json      — банк улиц
src/data/botResponses.json — фразы бота
src/data/drivers.json      — описания водителей
src/data/pushTexts.json    — push-уведомления
public/assets/             — картинки, GIF, логотип
```

---

## Общие правила

1. **Стор (`gameStore.js`) — общий контракт.** Не меняйте его без согласования. Если нужно добавить поле — обсудите.
2. **CSS-переменные** уже настроены через `data-level` на `<html>`. Используйте `var(--accent)`, `var(--text-primary)` и т.д.
3. **Заглушки** уже стоят — заменяйте на реальные компоненты.
4. **Каждый компонент** имеет TODO-комментарий с именем ответственного.
5. **Коммитьте часто**, пишите осмысленные commit messages.

---

## Приоритеты (MVP → Nice-to-have)

### 🔴 Must have (первые 3 часа)

1. Капча + регистрация работает (Участник 1)
2. Главный экран с рекламой + убегающей кнопкой (Участник 2)
3. Рулетка водителей крутится (Участник 2)
4. Судоку при отмене работает (Участник 3)
5. Магазин покупает улучшения (Участник 4)
6. ЯР начисляются/списываются, уровни меняются (все)
7. Питч-слайды готовы (Участник 5)

### 🟡 Should have (следующие 2 часа)

1. Web Speech API — лицензия (Участник 1)
2. d20 кубик с анимацией (Участник 2)
3. Педали во время ожидания (Участник 3)
4. Колода карточек красивая (Участник 4)
5. Все 5 уровней UI работают визуально (Участник 2 + 4)

### 🟢 Nice to have (оставшееся время)

1. CV-жест «67» (Участник 3)
2. Таксист собирает покемонов (Участник 3)
3. Боготворение Яндекса (Участник 4)
4. Похмелье + амнезия + алкотестер (Участник 3 + 4)
5. Отряд «ВМЕСТЕ» и VPN-Нидерланды (Участник 2)
6. Деплой на Vercel (Участник 5)
